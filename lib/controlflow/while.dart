void main() {
  // The while loop checks the condition first. It runs as long as the condition is true.
  int count = 1;
  while (count <= 5) {
    print('Count: $count');
    count++; // increase the value each time
  }
}